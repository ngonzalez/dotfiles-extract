#! /bin/bash

file=$1

if [ -z "${file}" ]; then
    echo "Missing file parameter. Usage: ~/.setfile <FILE_OR_DIRECTORY>"
    exit 1
fi

d=`/usr/bin/GetFileInfo -m ${file}`
/usr/bin/SetFile -d "${d}" ${file}

exit 0
