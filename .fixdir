#! /bin/bash
cd $1
file=`ls | grep -iv complete | tail -1`
modifiedDate=`/usr/bin/GetFileInfo -m "$file"`
cd ..
/usr/bin/SetFile -d "$modifiedDate" $1
/usr/bin/SetFile -m "$modifiedDate" $1
echo "---"
echo "$1"
echo "$modifiedDate"
