#! /bin/bash

file=$1

if [ -z "${file}" ]; then
    echo "Missing file parameter. Usage: ~/.setfile <FILE>"
    exit 1
fi

if [ ! -f "${file}" ]; then
    exit 1
fi

d=`/usr/bin/GetFileInfo -d ${file}`
/usr/bin/SetFile -m "${d}" ${file}

exit 0
